# The Solana wallet-adapter / Reown stack and Privy pull different (peerOptional)
# versions of `ox`; the clash is harmless. Pin legacy peer resolution so installs
# are reproducible locally and on Vercel.
legacy-peer-deps=true
