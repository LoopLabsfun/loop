{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git log -1 --pretty=%B | grep -qx '\\[no-deploy\\]' && exit 0 || exit 1",
  "crons": [
    { "path": "/api/agent/cron", "schedule": "0 0 * * *" },
    { "path": "/api/hood-buybot", "schedule": "*/2 * * * *" }
  ]
}
